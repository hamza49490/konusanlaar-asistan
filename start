import subprocess

import os
GITHUB_TOKEN = os.getenv('GITHUB_TOKEN')

curl_command = f'curl -H "Authorization: token {GITHUB_TOKEN}" https://api.github.com/Hamza49490'
subprocess.run(curl_command, shell=True)

python3 -m AnonXMusic
